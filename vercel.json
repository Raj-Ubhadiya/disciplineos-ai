{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack pnpm --filter @disciplineos/web build",
  "outputDirectory": "apps/web/.next"
}
